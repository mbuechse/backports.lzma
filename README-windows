Place the include und lib folders from the XZ windows build into the windows directory.
If you are using devpi, add --no-vcs to the upload command; otherwise it will
not include the these directories, for they are not in the git repository.